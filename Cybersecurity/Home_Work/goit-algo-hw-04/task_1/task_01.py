"""
    Перше завдання
    
У вас є текстовий файл, який містить інформацію про місячні заробітні плати розробників у вашій компанії. Кожен рядок у файлі містить прізвище розробника та його заробітну плату, які розділені комою без пробілів.

Наприклад:
Alex Korp,3000
Nikita Borisenko,2000
Sitarama Raju,1000

Ваше завдання - розробити функцію total_salary(path), яка аналізує цей файл і повертає загальну та середню суму заробітної плати всіх розробників.

Вимоги до завдання:
1. Функція total_salary(path) має приймати один аргумент - шлях до текстового файлу (path).
2. Файл містить дані про заробітні плати розробників, розділені комами. Кожен рядок вказує на одного розробника.
3. Функція повинна аналізувати файл, обчислювати загальну та середню суму заробітної плати.
4. Результатом роботи функції є кортеж із двох чисел: загальної суми зарплат і середньої заробітної плати.

Рекомендації для виконання:
1. Використовуйте менеджер контексту with для читання файлів.
2. Пам'ятайте про встановлення кодування при відкриті файлів
3. Для розділення даних у кожному рядку можна застосувати метод split(',').
4. Обрахуйте загальну суму заробітної плати, а потім розділіть її на кількість розробників, щоб отримати середню зарплату.
5. Опрацьовуйте можливі винятки при роботі з файлами, такі як відсутність файлу.

Критерії оцінювання:
1. Функція повинна точно обчислювати загальну та середню суми.
2. Повинна бути обробка випадків, коли файл відсутній або пошкоджений.
3. Код має бути чистим, добре структурованим і зрозумілим.

Приклад використання функції:
total, average = total_salary("path/to/salary_file.txt")
print(f"Загальна сума заробітної плати: {total}, Середня заробітна плата: {average}")

Очікуваний результат:
Загальна сума заробітної плати: 6000, Середня заробітна плата: 2000
"""

from pathlib import Path


def total_salary(path: str | Path) -> tuple[float, float]:
    """
    Ця функція аналізує файл із зарплатами та повертає загальну суму та середнє значення.
    :param path: Шлях до файлу (рядок або об'єкт Path)
    :return: Кортеж (загальна сума, середня зарплата)    
    """
    total = 0.0
    count = 0
    file_path = Path(path)  # Перетворимо в Path для зручності

    try:
        with file_path.open("r", encoding="utf-8") as file:
            for line in file:
                try:
                    # Очистимомо рядок і розділимо по комі
                    parts = line.strip().split(',', maxsplit=1)  
                    if len(parts) != 2:
                        continue  # Пропустио рядки, де немає пари "Ім'я,Зарплата"

                    salary = float(parts[1])  # Перетворимо другу частину в число
                    total += salary
                    count += 1
                    
                except ValueError:
                    # Якщо зарплата не є числом, просто пропустимо цей рядок
                    continue

    except FileNotFoundError:
        print(f"Помилка: Файл не знайдено за шляхом '{file_path.absolute()}'")
        return 0.0, 0.0
    except Exception as e:
        print(f"Непередбачувана помилка: {e}")
        return 0.0, 0.0

    # Уникаємо ділення на нуль, якщо файл порожній або всі рядки некоректні
    if count == 0:
        return 0.0, 0.0

    average = total / count
    return total, average


# --- Блок тестування (виконується тільки якщо запустити цей файл) ---
if __name__ == "__main__":
    # 1. Автоматичне створення тестових даних (використовуючи pathlib)
    current_dir = Path(__file__).parent
    data_dir = current_dir / "data"
    data_dir.mkdir(exist_ok=True)  # Створимо папку, якщо її немає
    
    file_path = data_dir / "salary_file.txt"
    
    # Запишемо тестові дані
    content = """Alex Korp,3000
Nikita Borisenko,2000
Sitarama Raju,1000
Broken Line without comma
John Doe,NotANumber
Jane Smith,4000"""
    
    with file_path.open("w", encoding="utf-8") as f:
        f.write(content)
        
    print(f"Файл створено: {file_path}")

    # 2. Виклик функції
    total, average = total_salary(file_path)
    print(f"\nРезультат:")
    print(f"Загальна сума: {total}")
    print(f"Середня зарплата: {average}")  # Має бути (3000+2000+1000+4000)/4 = 2500.0
