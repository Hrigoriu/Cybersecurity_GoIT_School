-- Завдання: Знайти середній бал на потоці (по всій таблиці оцінок).

-- Це найпростіший запит з використанням агрегатної функції.
-- Оскільки ми не використовуємо GROUP BY, AVG порахує середнє для ВСІХ рядків таблиці grades.
SELECT ROUND(AVG(grade), 2) AS overall_avg_grade
FROM grades;