-- Завдання: Знайти, які курси читає певний викладач (наприклад, викладач з ID=1).

-- Вибираємо назву предмета
SELECT sub.name
FROM subjects sub
-- Фільтруємо таблицю предметів, залишаючи лише ті, де ID викладача дорівнює 1.
WHERE sub.teacher_id = 1;